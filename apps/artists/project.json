{
  "name": "artists",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/artists/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/artists",
        "main": "apps/artists/src/main.ts",
        "tsConfig": "apps/artists/tsconfig.app.json",
        "assets": ["apps/artists/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/artists/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "artists:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "artists:build:development"
        },
        "production": {
          "buildTarget": "artists:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/artists/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/artists/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
